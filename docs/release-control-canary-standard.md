# Signed release-control standard-risk canary

This temporary documentation-only change verifies that the autonomous Radulator release control plane classifies a non-clinical change as standard risk and requires exactly the primary signed judge attestation.

It is a non-merging activation canary and does not alter calculator behavior, clinical guidance, deployment configuration, or production content.
