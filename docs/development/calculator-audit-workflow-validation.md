# Reusable calculator-audit reference — validation2026-09-08

User requested capture of the ALBI audit workflow, explicit US-guideline checks
and inspection of Hermes lifecycle consumers. This is a tooling-validation
receipt, not a new calculator completion tracker.

## Authoritative source and installation

- Maintained source: `ops/hermes/radulator/skills/radulator-calculator-audit/` in
  this control change. `AGENTS.md` links the entrypoint for full audit/closeout.
- Local Codex installation: `/Users/mohibhafeez/.codex/skills/radulator-calculator-audit/`.
  Installed as a byte-identical copy; edit the repository source, then explicitly
  synchronize and compare the local copy. Do not maintain divergent versions.
- No symlink depends on a removable worktree. The skill refers to the actual
  checkout/revision rather than assuming the candidate is already on main.
- No Mac mini skill installation, installer mapping, job activation, registry
  synchronization or retained-learning acknowledgment is claimed. These require
  separate selective integration and readback; the existing full overlay is not
  reinstalled because it could overwrite customized judge prompts.

## Validation checklist and results

- [x] Selected a narrowly triggered project reference skill, not a new agent
  platform, discipline framework or permission change.
- [x] Captured observed gaps from the real audit: owner/reviewer source-access
  mismatch, missing explicit US-guidance slot, DOM-only print check missing PDF
  defects, stale metadata expectations, and unsynchronized planning consumers.
- [x] Ran six independent pre-skill application scenarios (A–F below). All six
  decisions were correct. **No baseline judgment failure or measured speed/token
  improvement is claimed.** The purpose is cheaper retrieval/reuse of concrete
  project knowledge, not evidence of a smarter model.
- [x] Wrote minimal frontmatter/entrypoint plus workflow and lifecycle references;
  provided an ALBI example without exporting its clinical constants to other tools.
- [x] Ran the same six cases with the skill:6/6, no decision regression. Reviewer
  also inspected research commands, source paths and installation boundaries.
- [x] Fixed two concrete routing defects: ALBI record may be absent in an older
  control checkout; three lifecycle skill paths lacked their repository prefix.
  Clarified the installed profile root and record-JSON research argument.
- [x] Independent recheck accepted the path/revision corrections with no new
  routing defect in those sections.
- [x] Bundled `quick_validate.py` passes for repository and installed skill;
  recursive comparison confirms the local copy matches. Git whitespace check passes.
- [x] Pressure/rationalization micro-tests with five repetitions are not applicable:
  this is a project reference/technical workflow, not new behavior-shaping wording.
  No claim that the skill is universally proven or automatically prevents duplicates.

| Scenario | Expected disposition, passed before and after skill |
|---|---|
| A: Paper/EASL reviewed, TACE advice, US alignment asked | Check exact advice against applicable current US guidance; no blanket alignment claim |
| B: DOM print visible,38 tests green, no PDF viewed | Render and inspect actual PDF before PDF acceptance |
| C: Review date changed, old inventory assertion | Repair justified metadata consumer and rerun; preserve valid clinical evidence |
| D: CI green, independent source retrieval blocked | Supply legitimate reviewable evidence and obtain independent decision; hold release |
| E: Registry verified, pending receipt, stale rewrite seed | Resolve actual canonical/live state; no rewrite based only on stale seed |
| F: GitHub closure, stale installed registry, paused learning | Keep calculator closure distinct from unproven remote synchronization/retention |

## Operational boundaries retained

One calculator implementation remains active. ALBI's clinical/evidence correction
stays in PR265; this workflow capture stays in the separate control worktree.
Protected gates, reviewer identities/keys, schedules and production are unchanged
by the skill installation. Skill instructions are not deterministic enforcement.
The lifecycle reference records the concrete integration gaps and smallest
follow-up; it does not silently count them as fixed.
